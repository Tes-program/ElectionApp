# Server Configuration
server.port=8080

# MongoDB Configuration
spring.mongodb.host=localhost
spring.mongodb.port=27017
spring.mongodb.database=electionapp

# Application Name
spring.application.name=ElectionApp

# Logging
logging.level.dreamdev.moniepoint=INFO
